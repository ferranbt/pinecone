# Development Guidelines

## Parser Error Handling

When encountering syntax parsing errors in this PineScript parser repository, follow the workflow documented in `.claude/parsing-workflow.md`.

## Pine Script Reference

When implementing or debugging Pine Script language features (built-in functions, variables, operators, etc.), **always consult the official reference documentation** using the `just ref` command. See `.claude/pine-reference.md` for detailed usage instructions.
