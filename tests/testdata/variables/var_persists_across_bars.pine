// Pine `var`: the initializer runs only the first time execution reaches the
// declaration, and the variable persists across bars. So the counter keeps
// incrementing bar to bar instead of resetting to 0 each bar.

// Bars: 3
var count = 0
count := count + 1
log.info(count)

// Expected output:
// 1
// 2
// 3
