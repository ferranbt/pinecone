// Contrast to `var`: a plain declaration re-evaluates its initializer every
// bar, so `count` is reset to 0 each bar and the increment always yields 1.

// Bars: 3
count = 0
count := count + 1
log.info(str.tostring(count))

// Expected output:
// 1
// 1
// 1
