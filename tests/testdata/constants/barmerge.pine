// barmerge.* constants resolve to their string tag.
log.info(str.tostring(barmerge.gaps_on))
log.info(str.tostring(barmerge.gaps_off))
log.info(str.tostring(barmerge.lookahead_on))
log.info(str.tostring(barmerge.lookahead_off))

// Expected output:
// gaps_on
// gaps_off
// lookahead_on
// lookahead_off
