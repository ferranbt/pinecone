// barmerge.* constants resolve to their string tag.
log.info(barmerge.gaps_on)
log.info(barmerge.gaps_off)
log.info(barmerge.lookahead_on)
log.info(barmerge.lookahead_off)

// Expected output:
// gaps_on
// gaps_off
// lookahead_on
// lookahead_off
