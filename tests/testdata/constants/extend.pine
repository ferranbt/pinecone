//@version=5
indicator("constants/extend")
// extend.* constants resolve to their string tag.
log.info(str.tostring(extend.none))
log.info(str.tostring(extend.left))
log.info(str.tostring(extend.right))
log.info(str.tostring(extend.both))

// Expected output:
// none
// left
// right
// both
