// position.* constants resolve to their string tag.
log.info(str.tostring(position.top_left))
log.info(str.tostring(position.top_right))
log.info(str.tostring(position.middle_center))
log.info(str.tostring(position.bottom_right))
log.info(str.tostring(position.bottom))

// Expected output:
// top_left
// top_right
// middle_center
// bottom_right
// bottom
