//@version=5
indicator("constants/xloc")
// xloc.* constants resolve to their string tag.
log.info(str.tostring(xloc.bar_index))
log.info(str.tostring(xloc.bar_time))

// Expected output:
// bar_index
// bar_time
