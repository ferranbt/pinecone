//@version=5
indicator("constants/format")
// format.* constants resolve to their string tag.
log.info(str.tostring(format.inherit))
log.info(str.tostring(format.price))
log.info(str.tostring(format.volume))
log.info(str.tostring(format.percent))
log.info(str.tostring(format.mintick))

// Expected output:
// inherit
// price
// volume
// percent
// mintick
