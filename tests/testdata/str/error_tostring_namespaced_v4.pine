//@version=4
// `str.tostring` does not exist in v4 — it is the v5 replacement for the global
// `tostring`, so reaching for it here must fail rather than silently resolve.
text = str.tostring(42)

// Expected error: Object has no member 'tostring'
