//@version=5
indicator("arguments/error_unknown_named_arg")
// An unknown named argument is rejected by semantic analysis, before the script
// runs, using the parameters the builtin declares.

p = plot(series=100.0, invalid_param=200.0)

// Expected error:
// error [unknown-argument] 6:9: `plot` has no argument named `invalid_param`
