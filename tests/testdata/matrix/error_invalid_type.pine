//@version=5
indicator("matrix/error_invalid_type")
// Test that matrix.new with invalid type fails
m = matrix.new<foobar>(2, 3, 0.0)
log.info(str.tostring(m))

// Expected error: Invalid matrix element type
