// A false left operand does not decide `or`, so the right operand IS evaluated.
// Here that reaches the undefined `boom`, which errors — proving the operand
// was not skipped.
x = false or boom

// Expected error: not found
