// A version this toolchain cannot compile must be rejected, not silently
// treated as current Pine.
//@version=2
study("basics/version_unsupported")
x = 1

// Expected error: unsupported Pine version 2
