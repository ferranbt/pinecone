//@version=6
indicator("ta/lowest_dynamic_length")
// ta.lowest tracks a dynamic (series) length as the window grows and shrinks.
// Bars: 5
var int n = 0
n := n + 1
v = n * 10
len = n % 2 == 1 ? 1 : 2
log.info(str.tostring(ta.lowest(v, len)))

// Expected output:
// 10
// 10
// 30
// 30
// 50
